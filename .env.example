# Fireberry Integration (required for live mode)
# Get these values from your Fireberry CRM instance
FIREBERRY_BASE_URL=https://your-fireberry-instance.com
FIREBERRY_API_KEY=your-api-key-here

# App Configuration
MOCK_MODE=true                    # Set to false to use live Fireberry data
DEBUG=false                       # Enable debug logging for development

# Optional: Site URL for server-side requests
# Useful when running in production or behind a proxy
SITE_URL=http://localhost:4321

# Additional environment variables for future features:
# DATABASE_URL=                   # For persistent storage
# SESSION_SECRET=                 # For authentication
# EMAIL_FROM=                     # For notifications
# SMTP_HOST=                      # For email sending
# SMTP_USER=                      # For email authentication
# SMTP_PASS=                      # For email authentication
